# Morse Light / 摩斯电码手电筒

![License](https://img.shields.io/badge/license-MIT-blue.svg) ![Version](https://img.shields.io/badge/version-0.0.0-green.svg)

[English](#english) | [中文](#chinese)

---

<a name="english"></a>
## 🇬🇧 English

### Project Overview
**Morse Light** is a cross-platform mobile application built with React, Vite, and Capacitor. It allows users to transmit Morse code messages using their device's flashlight or screen. Designed for emergency situations and educational purposes, it features a clean, modern UI, internationalization support, and a dedicated tutorial section.

### Features

#### Functional Features
- **Text-to-Morse Conversion**: Real-time conversion of text input into Morse code signals.
- **Flashlight Transmission**: Uses the device's hardware flashlight to transmit Morse code signals (requires permission).
- **Screen Flash Fallback**: Automatically falls back to flashing the screen white if the flashlight is unavailable or disabled.
- **Common Phrases**: Quick access to predefined emergency signals like SOS, HELP, WATER, etc.
- **Interactive Tutorial**: Step-by-step guide on how to use the app and understand basic Morse code.
- **Bilingual Support**: Full support for English and Chinese (Simplified), configurable in settings.
- **Playback Control**: Start and stop transmission at any time.

#### Technical Features
- **Cross-Platform**: Runs on iOS, Android, and Web (PWA).
- **Modern Tech Stack**: Built with React 18, TypeScript, and Vite for fast development and performance.
- **Capacitor Integration**: Native device feature access (Flashlight) via Capacitor plugins.
- **Custom Plugin**: Includes a local Capacitor plugin for flashlight control (`plugins/flashlight`).
- **Styling**: Utility-first styling with Tailwind CSS and `clsx` for conditional classes.
- **State Management**: React Context API for global state management.

### Architecture Overview
The project follows a standard React application structure powered by Vite:

- **`src/`**: Source code root.
  - **`components/`**: Reusable UI components.
  - **`pages/`**: Main application screens (Home, Phrases, Tutorial).
  - **`hooks/`**: Custom React hooks (e.g., `useFlashlight`).
  - **`utils/`**: Utility functions (Morse code logic, timing constants).
  - **`i18n/`**: Internationalization configuration and translation files.
  - **`contexts/`**: React Context providers.
- **`plugins/`**: Local Capacitor plugins (e.g., `flashlight`).
- **`ios/`**: Native iOS project files (generated by Capacitor).

### Installation & Running

#### Prerequisites
- Node.js (v18+ recommended)
- npm or yarn
- CocoaPods (for iOS development)
- Xcode (for iOS development)

#### Steps
1. **Clone the repository:**
   ```bash
   git clone <repository-url>
   cd morse-code-app
   ```

2. **Install dependencies:**
   ```bash
   npm install
   ```

3. **Run in development mode (Web):**
   ```bash
   npm run dev
   ```
   Open your browser at the URL shown (usually `http://localhost:5173`).

4. **Build for production:**
   ```bash
   npm run build
   ```

### Deployment

#### iOS
1. **Sync Capacitor config:**
   ```bash
   npx cap sync ios
   ```

2. **Open in Xcode:**
   ```bash
   npx cap open ios
   ```

3. **Build & Run:**
   - Select your target device or simulator in Xcode.
   - Click the "Run" (Play) button.
   *Note: Physical device required for flashlight testing.*

### Usage Examples
1. **Sending a Custom Message:**
   - Go to the **Home** tab.
   - Type "HELLO" in the text box.
   - Tap **START TRANSMISSION**.
   - The flashlight (or screen) will flash the Morse code for "HELLO".

2. **Sending an SOS:**
   - Go to the **Phrases** tab.
   - Tap on the **SOS** card.
   - The app will immediately start transmitting the SOS signal.

### Configuration
- **Language**: Change between English and Chinese in **Settings**.
- **Transmission Mode**: Toggle between "Flashlight" and "Screen Flash" in **Settings** (if supported).

### Testing
Currently, the project relies on manual testing.
- **Unit Tests**: Not yet implemented.
- **Manual Verification**:
  - Verify flashlight toggles correctly on a real device.
  - Verify screen flashes white when flashlight is disabled.
  - Check all translations in both languages.

### Contributing
Contributions are welcome! Please follow these steps:
1. Fork the repository.
2. Create a feature branch (`git checkout -b feature/AmazingFeature`).
3. Commit your changes (`git commit -m 'Add some AmazingFeature'`).
4. Push to the branch (`git push origin feature/AmazingFeature`).
5. Open a Pull Request.

### License
Distributed under the MIT License. See `LICENSE` for more information.

### FAQ
**Q: Why doesn't the flashlight work on my simulator?**
A: Simulators do not have hardware flashlights. Please test on a physical device or use the Screen Flash mode.

**Q: Can I add more languages?**
A: Yes! Add the new language type to `src/i18n/translations.ts` and update the settings component.

---

<a name="chinese"></a>
## 🇨🇳 中文

### 项目概述
**摩斯电码手电筒 (Morse Light)** 是一款基于 React、Vite 和 Capacitor 构建的跨平台移动应用程序。它允许用户使用设备的闪光灯或屏幕发送摩斯电码信息。该应用专为紧急情况和教育目的设计，拥有简洁现代的用户界面，支持多语言，并包含专门的教程部分。

### 功能特性

#### 功能特点
- **文本转摩斯电码**：将输入的文本实时转换为摩斯电码信号。
- **手电筒传输**：使用设备的硬件闪光灯发送摩斯电码信号（需要权限）。
- **屏幕闪烁备用**：如果手电筒不可用或被禁用，自动切换为屏幕白色闪烁。
- **常用短语**：快速访问预定义的紧急信号，如 SOS、HELP、WATER 等。
- **交互式教程**：逐步指导如何使用应用程序和理解基本摩斯电码。
- **双语支持**：完全支持英语和简体中文，可在设置中配置。
- **播放控制**：随时开始和停止传输。

#### 技术特点
- **跨平台**：可在 iOS、Android 和 Web (PWA) 上运行。
- **现代技术栈**：使用 React 18、TypeScript 和 Vite 构建，开发迅速，性能优越。
- **Capacitor 集成**：通过 Capacitor 插件访问原生设备功能（闪光灯）。
- **自定义插件**：包含用于手电筒控制的本地 Capacitor 插件 (`plugins/flashlight`)。
- **样式设计**：使用 Tailwind CSS 和 `clsx` 进行实用优先的样式设计。
- **状态管理**：使用 React Context API 进行全局状态管理。

### 架构概览
本项目遵循由 Vite 驱动的标准 React 应用程序结构：

- **`src/`**：源代码根目录。
  - **`components/`**：可复用的 UI 组件。
  - **`pages/`**：主要应用屏幕（首页、短语、教程）。
  - **`hooks/`**：自定义 React Hooks（例如 `useFlashlight`）。
  - **`utils/`**：工具函数（摩斯电码逻辑、计时常量）。
  - **`i18n/`**：国际化配置和翻译文件。
  - **`contexts/`**：React Context 提供者。
- **`plugins/`**：本地 Capacitor 插件（例如 `flashlight`）。
- **`ios/`**：原生 iOS 项目文件（由 Capacitor 生成）。

### 安装与运行

####以此为前提
- Node.js (建议 v18+)
- npm 或 yarn
- CocoaPods (用于 iOS 开发)
- Xcode (用于 iOS 开发)

#### 步骤
1. **克隆仓库：**
   ```bash
   git clone <repository-url>
   cd morse-code-app
   ```

2. **安装依赖：**
   ```bash
   npm install
   ```

3. **运行开发模式 (Web)：**
   ```bash
   npm run dev
   ```
   在浏览器中打开显示的 URL（通常是 `http://localhost:5173`）。

4. **构建生产版本：**
   ```bash
   npm run build
   ```

### 部署

#### iOS
1. **同步 Capacitor 配置：**
   ```bash
   npx cap sync ios
   ```

2. **在 Xcode 中打开：**
   ```bash
   npx cap open ios
   ```

3. **构建并运行：**
   - 在 Xcode 中选择目标设备或模拟器。
   - 点击 "Run" (播放) 按钮。
   *注意：手电筒测试需要真机。*

### 使用示例
1. **发送自定义消息：**
   - 转到 **首页 (Home)** 标签。
   - 在文本框中输入 "HELLO"。
   - 点击 **开始传输 (START TRANSMISSION)**。
   - 手电筒（或屏幕）将闪烁 "HELLO" 的摩斯电码。

2. **发送 SOS：**
   - 转到 **短语 (Phrases)** 标签。
   - 点击 **SOS** 卡片。
   - 应用将立即开始传输 SOS 信号。

### 配置选项
- **语言**：在 **设置** 中切换英语和中文。
- **传输模式**：在 **设置** 中切换 "手电筒" 和 "屏幕闪烁"（如果支持）。

### 测试
目前，项目依赖于手动测试。
- **单元测试**：尚未实施。
- **手动验证**：
  - 在真机上验证手电筒是否正确开关。
  - 验证禁用手电筒时屏幕是否闪烁白色。
  - 检查两种语言的所有翻译是否正确。

### 贡献指南
欢迎贡献！请遵循以下步骤：
1. Fork 本仓库。
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)。
3. 提交您的更改 (`git commit -m 'Add some AmazingFeature'`)。
4. 推送到分支 (`git push origin feature/AmazingFeature`)。
5. 提交 Pull Request。

### 许可协议
本项目基于 MIT 许可证分发。详情请参阅 `LICENSE` 文件。

### 常见问题解答 (FAQ)
**问：为什么手电筒在我的模拟器上不工作？**
答：模拟器没有硬件手电筒。请在真机上测试或使用屏幕闪烁模式。

**问：我可以添加更多语言吗？**
答：可以！在 `src/i18n/translations.ts` 中添加新的语言类型并更新设置组件即可。
